apiVersion: v1
kind: Service
metadata:
  name: {{ include "linkerd.fullname" . }}
  labels:
    {{- include "linkerd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "linkerd.selectorLabels" . | nindent 4 }}
